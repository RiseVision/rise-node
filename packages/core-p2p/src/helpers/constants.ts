export const constants = {
  broadcastInterval: 5000,
  broadcastLimit   : 20,
  parallelLimit    : 20,
  releaseLimit     : 25,
  relayLimit       : 2,
};

export type P2PConstantsType = typeof constants;
