syntax = "proto3";

message singleTransportTx {
    bytes tx = 1;
    uint32 relays = 2;
}
message transportTransactions {
    repeated singleTransportTx transactions = 1;
}

message transportTx {
    bytes data = 1;
    repeated bytes buffers = 2;
}
