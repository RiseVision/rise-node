syntax = "proto3";
import "./blocks.bytes.proto";

message transportBlocks {
    repeated bytes blocks = 1;
}

message transportBlock {
    bytes block = 1;
}

message commonBlock {
    bytes common = 1;
}

message postTransportBlockResponse {
    bool success = 1;
    string blockId = 2;
}
